pod:
  containers:
  - name: build
    image: golang:1.13-alpine
    workingDir: /workspace
    imagePullPolicy: IfNotPresent
    command:
      - sh 
      - -c
      - |
        apk add --no-cache sed
        cd /workspace
        ls
        sleep 31
