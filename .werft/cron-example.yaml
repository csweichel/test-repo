plugins:
  cron: "30 * * * *"
pod:
  containers:
  - name: build
    image: golang:1.13-alpine
    workingDir: /workspace
    imagePullPolicy: IfNotPresent
    command:
      - sh 
      - -c
      - |
        sleep 5
        echo "cron job"
